apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: inventory-service
  labels:
    app: inventory-service
spec:
  selector:
    app: inventory-pod-deploy
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080

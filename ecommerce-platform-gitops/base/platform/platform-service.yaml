apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: platform-service
  labels:
    app: platform-service
spec:
  selector:
    app: platform-pod-deploy
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080

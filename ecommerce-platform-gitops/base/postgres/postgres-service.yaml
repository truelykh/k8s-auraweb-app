apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: postgres-service
  labels:
    app: postgres-service
spec:
  selector:
    app: postgres-pod-deploy
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432

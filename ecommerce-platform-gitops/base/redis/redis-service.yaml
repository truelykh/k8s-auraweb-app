apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: redis-service
  labels:
    app: redis-service
spec:
  selector:
    app: redis-pod-deploy
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379

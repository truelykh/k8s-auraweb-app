apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: auraweb
  name: ful-deploy
  labels:
    app: ful-deploy
spec:
  selector:
    matchLabels:
      app: ful-pod-deploy
  template:
    metadata:
      labels:
        app: ful-pod-deploy
    spec:
      serviceAccountName: fulservice
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsUser: 1000
        runAsNonRoot: true
      containers:
        - name: ful
          image: kubeprojects/fulfilment:1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3003"
            - name: DB_HOST
              value: postgres-service
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: auraweb_db
            - name: DB_USER
              value: auraweb_user
            - name: DB_PASSWORD
              value: password
            - name: REDIS_HOST
              value: redis-service
            - name: RABBITMQ_URL
              value: amqp://rabbitmq-service:5672
          ports:
            - containerPort: 3003
          resources:
            requests:
              cpu: "64m"
              memory: "256Mi"
            limits:
              cpu: "256m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop:
                - ALL
          readinessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /health
              port: 3003
          livenessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /health
              port: 3003

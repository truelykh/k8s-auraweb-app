apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: auraweb
  name: admin-deploy
  labels:
    app: admin-deploy
spec:
  selector:
    matchLabels:
      app: admin-pod-deploy
  template:
    metadata:
      labels:
        app: admin-pod-deploy
    spec:
      serviceAccountName: adminservice
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        fsGroup: 1000
      containers:
        - name: admin
          image: kubeprojects/admin:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: "64Mi"
              memory: "50m"
            limits:
              cpu: "128Mi"
              memory: "200m"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop:
                - ALL
          readinessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /
              port: 8080
          livenessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /
              port: 8080

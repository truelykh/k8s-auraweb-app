apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: catalogue-service
  labels:
    app: catalogue-service
spec:
  selector:
    app: catalogue-pod-deploy
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 3001

apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: auraweb
  name: catalogue-deploy
  labels:
    app: catalogue-deploy
spec:
  selector:
    matchLabels:
      app: catalogue-pod-deploy
  template:
    metadata:
      labels:
        app: catalogue-pod-deploy
    spec:
      serviceAccountName: catalogueservice
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsUser: 1000
        runAsNonRoot: true
      containers:
        - name: catalogue
          image: kubeprojects/catalog:1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3001"
            - name: DB_HOST
              value: postgres-service
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: auraweb_db
            - name: DB_USER
              value: auraweb_user
            - name: DB_PASSWORD
              value: password
            - name: REDIS_URL
              value: redis://redis-service:6379
            - name: RABBITMQ_URL
              value: amqp://rabbitmq-service:5672
          ports:
            - containerPort: 3001
          resources:
            requests:
              cpu: "64m"
              memory: "256Mi"
            limits:
              cpu: "256m"
              memory: "512Mi"
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            capabilities:
              drop:
                - ALL
          readinessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /health
              port: 3001
          livenessProbe:
            initialDelaySeconds: 20
            periodSeconds: 15
            httpGet:
              path: /health
              port: 3001

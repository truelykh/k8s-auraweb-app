apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: shopping-service
  labels:
    app: shopping-service
spec:
  selector:
    app: shopping-pod-deploy
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080

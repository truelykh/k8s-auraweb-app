apiVersion: v1
kind: Service
metadata:
  namespace: auraweb
  name: frontend-service
  labels:
    app: frontend-service
spec:
  selector:
    app: frontend-pod-deploy
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
